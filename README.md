## 📦 Daftar Plugin Kustom

Konfigurasi ini memuat *plugin* di bawah, diorganisir berdasarkan fungsionalitasnya:

### 1. 🖥️ Core Development & Language Support

| Plugin | Keterangan |
| :--- | :--- |
| `nvim-lspconfig` | Konfigurasi inti untuk Language Server Protocol (LSP). |
| `mason` | Manajer packet universal untuk menginstal LSP, *linter*, dan *formatter*. |
| `nvim-cmp` | Mesin *autocompletion* yang kuat. |

### 2. 🖋️ Formatting & Linting

| Plugin | Keterangan |
| :--- | :--- |
| `conform` | *Plugin* untuk *formatting* kode yang fleksibel. |
| `nvim-lint` | *Plugin* *linting* asinkron. |

### 3. 🐛 Debugging (DAP)

| Plugin | Keterangan |
| :--- | :--- |
| `nvim-dap` | Inti dari integrasi Debug Adapter Protocol (DAP). |
| `nvim-dap-python` | Konfigurasi spesifik untuk *debugging* proyek Python. |

### 4. 📝 Editor Enhancements

| Plugin | Keterangan |
| :--- | :--- |
| `nvim-treesitter` | Parsing dan *syntax highlighting* yang canggih. |
| `colorizer` | Menampilkan warna (HEX, RGB, dll.) secara langsung di editor. |
| `hlchunk` | Visualisasi blok kode saat ini (seperti *indent guide* yang lebih pintar). |
| `triforce` | (Tambahkan deskripsi fungsionalitas plugin ini) |

### 5. 📂 Navigasi & Utility

| Plugin | Keterangan |
| :--- | :--- |
| `nvim-tree` | *File explorer* yang terintegrasi (tree-view). |
| `jqx` | Alat untuk memformat dan melihat JSON/YAML. |
| `floaterm` | Terminal *floating* yang mudah diakses. |
| `menu` | (Tambahkan deskripsi fungsionalitas plugin ini) |

### 6. ⚙️ Produktivitas & Pelaporan

| Plugin | Keterangan |
| :--- | :--- |
| `todo-comments` | Menyorot komentar tugas (`TODO`, `FIXME`, dll.) di kode. |
| `trouble` | Alat untuk menavigasi daftar lokasi (diagnostik, hasil *grep*, dll.) dengan mudah. |
| `markview` | *Live preview* Markdown di Nvim. |
| `volt` | (Tambahkan deskripsi fungsionalitas plugin ini) |
| `timerly` | (Tambahkan deskripsi fungsionalitas plugin ini) |
| `vim-wakatime` | Pelacakan waktu coding otomatis. |
| `git-blame` | Menampilkan informasi `git blame` di baris saat ini. |



## explore nvim

- Pahami struktur nvchad
- Install plugin yang di perlukan
- General stuff
- LSP
- code formatter : golang, php, javascript, typescript, erlang, rust, elixir,
- snippet
- module and refactor code nvim

## Kendala

- Mason masih belum bisa terinstall dengan deklaratif



